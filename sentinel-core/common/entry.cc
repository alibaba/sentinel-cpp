
#include <memory>

#include "sentinel-core/common/entry.h"

namespace Sentinel {

// void Entry::Exit(int count) {
//   if (!is_exit_) {
//     Slot::GlobalSlotChain->Exit(context_, resource_, count);
//     is_exit_ = true;
//   }
// }

// void Entry::Exit() { Exit(1); }

}  // namespace Sentinel
